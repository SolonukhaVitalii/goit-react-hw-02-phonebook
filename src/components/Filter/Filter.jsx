import React from 'react';
//import PropTypes from 'prop-types';

const Filter = ({ onChange }) => (
        <label htmlFor="">Find contacts by name<input type="text" /></label>
    );


Filter.propTypes = {
    
};

export default Filter;